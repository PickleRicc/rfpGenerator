# PRODUCT REQUIREMENTS DOCUMENT
## Federal Proposal Automation Platform - Multi-Agent System

**Version:** 1.0  
**Date:** December 2024  
**Status:** Scoping for Development

---

## 1. PRODUCT OVERVIEW

### **Vision**
Automate 80% of federal proposal writing work, reducing 80-120 hours of manual effort to 10-14 hours while achieving 85%+ expert compliance rating.

### **Problem Statement**
Federal proposal writing currently requires:
- 80-120 hours of manual effort per proposal
- Expert knowledge of compliance requirements
- Tedious formatting and repetitive content creation
- High risk of disqualifying errors (wrong format, missing requirements)

### **Solution**
8-agent AI system that generates compliant, human-quality federal proposals from RFP + client data, with human review checkpoints.

### **Success Metrics**
- Time reduction: 80-120 hours → 10-14 hours (85% reduction)
- Compliance score: 85%+ on expert review
- Win rate: Track proposal wins vs. losses
- Zero disqualifying errors in final output

---

## 2. USER STORIES

### **Primary User: Proposal Manager/Founder**

**US-1:** As a proposal manager, I want to upload an RFP and have the system extract all requirements automatically, so I don't spend 4 hours parsing the RFP manually.

**US-2:** As a proposal manager, I want the system to validate my company data before writing begins, so I catch missing certifications or expired documents early.

**US-3:** As a proposal manager, I want the system to generate 4 separate proposal volumes (not 1 combined document), so I don't get disqualified for format violations.

**US-4:** As a proposal manager, I want to review a compliance audit report before finalizing, so I can fix critical issues before submission.

**US-5:** As a proposal manager, I want to provide feedback on draft volumes and have revisions implemented automatically, so I maintain control over accuracy.

**US-6:** As a proposal manager, I want submission-ready PDFs + editable Word files, so I can make last-minute tweaks or submit directly.

---

## 3. SYSTEM ARCHITECTURE

### **Current Tech Stack**
- **Frontend:** Next.js (React)
- **Backend:** Supabase (PostgreSQL + Auth + Storage)
- **AI:** Claude API (Sonnet 4 via Anthropic)
- **File Processing:** Server-side PDF parsing, DOCX generation

### **Existing Database Schema**
```
companies
├── id (uuid, primary key)
├── name
├── contact_info
├── created_at
└── updated_at

labor_rates
├── id (uuid, primary key)
├── company_id (foreign key → companies)
├── category_name
├── year_1_rate
├── year_2_rate...
└── created_at

past_performance
├── id (uuid, primary key)
├── company_id (foreign key → companies)
├── contract_number
├── customer_agency
├── contract_value
├── poc_name / phone / email
└── performance_data (jsonb)

personnel
├── id (uuid, primary key)
├── company_id (foreign key → companies)
├── full_name
├── role
├── years_experience
├── clearance_level
├── certifications (jsonb)
└── resume_url

proposal_jobs
├── id (uuid, primary key)
├── company_id (foreign key → companies)
├── rfp_number
├── rfp_title
├── due_date
├── status (enum: draft, in_progress, review, complete)
├── created_at
└── updated_at

proposal_jobs_summary
├── id (uuid, primary key)
├── proposal_job_id (foreign key → proposal_jobs)
├── volume_1_url
├── volume_2_url
├── volume_3_url
├── volume_4_url
├── compliance_score
├── validation_report (jsonb)
└── created_at
```

---

## 4. FEATURE REQUIREMENTS

### **PHASE 1: RFP INTAKE & PARSING** (Agent 0 + 1)

**Feature 4.1: RFP Upload**
- User uploads RFP PDF via drag-and-drop interface
- System validates file (PDF only, < 50MB)
- System extracts text using PDF parser
- Stores RFP in Supabase Storage, metadata in `proposal_jobs`

**Feature 4.2: Agent 0 - Volume Creation**
- On project creation, Agent 0 creates 4 document containers in `proposal_jobs_summary`:
  - `volume_1_technical` (50-page limit)
  - `volume_2_management` (30-page limit)
  - `volume_3_past_performance` (25-page limit)
  - `volume_4_price` (no limit)
- Real-time page tracking per volume
- Hard stop at page limits

**Feature 4.3: Agent 1 - RFP Intelligence**
- Parse RFP into structured JSON:
  - Section L (submission requirements)
  - Section M (evaluation criteria)
  - Section C (technical requirements)
  - Section B (pricing CLINs)
- Generate compliance matrix (Excel export)
- Store in `proposal_jobs` as `rfp_parsed_data` (jsonb)
- Flag disqualifying requirements

**Database Updates:**
```sql
ALTER TABLE proposal_jobs ADD COLUMN rfp_file_url text;
ALTER TABLE proposal_jobs ADD COLUMN rfp_parsed_data jsonb;
ALTER TABLE proposal_jobs ADD COLUMN volume_page_limits jsonb; -- {vol_1: 50, vol_2: 30...}
```

---

### **PHASE 2: CLIENT DATA VALIDATION** (Agent 2)

**Feature 4.4: Client Intake Form**
- 7-section intake form (see CLIENT_INTAKE_FORM_REQUIREMENTS.md)
- Auto-save every 30 seconds
- Progress indicator (% complete)
- Inline validation (UEI format, required fields)

**Feature 4.5: Agent 2 - Data Validation**
- Query Supabase for company data:
  - `SELECT * FROM companies WHERE id = ?`
  - `SELECT * FROM past_performance WHERE company_id = ?`
  - `SELECT * FROM personnel WHERE company_id = ?`
  - `SELECT * FROM labor_rates WHERE company_id = ?`
- Run validation checks:
  - Placeholder detection (UEI != "ABC123...")
  - Expired certs (expiration_date > today + 90 days)
  - POC freshness (verified_date < 6 months ago)
  - Unrealistic claims (45 employees, 50 migrations)
- Generate validation report (stored in `proposal_jobs.validation_report`)
- Block proposal generation if BLOCKERS exist

**UI Component: Validation Report Dashboard**
```
STATUS: ⚠️ NEEDS ATTENTION

BLOCKERS:
❌ PM only has 8 years (need 10+)
[Fix] button → edit personnel form

WARNINGS:
⚠️ CISSP expires in 2 months
[Ignore] [Fix] buttons
```

**Database Updates:**
```sql
ALTER TABLE proposal_jobs ADD COLUMN validation_report jsonb;
ALTER TABLE proposal_jobs ADD COLUMN validation_status enum('pending', 'blocked', 'warnings', 'approved');
```

---

### **PHASE 3: CONTENT GENERATION** (Agent 3 + 4)

**Feature 4.6: Agent 3 - Content Mapping**
- Generate compliance matrix mapping requirements to volumes
- Create detailed outlines for all 4 volumes
- Allocate pages per section
- Store outlines in `proposal_jobs.content_outlines` (jsonb)

**Feature 4.7: Agent 4 - Parallel Writing**
- Spawn 4 sub-agents (4A, 4B, 4C, 4D)
- Each writes to separate volume
- Real-time page count monitoring
- Stop at page limits
- Store drafts in Supabase Storage:
  - `{proposal_id}/drafts/volume_1_draft.docx`

**UI Component: Generation Progress**
```
GENERATING PROPOSAL...

Agent 4A (Technical): ████████░░ 80% (40/50 pages)
Agent 4B (Management): ██████░░░░ 60% (18/30 pages)
Agent 4C (Past Perf): ███████░░░ 70% (14/20 pages)
Agent 4D (Price): ██████████ 100% (12 pages)

Estimated completion: 3 minutes
```

**Database Updates:**
```sql
ALTER TABLE proposal_jobs ADD COLUMN content_outlines jsonb;
ALTER TABLE proposal_jobs ADD COLUMN generation_progress jsonb; -- {vol_1: {pages: 40, status: 'writing'}}
```

---

### **PHASE 4: COMPLIANCE REVIEW** (Agent 5 + 6)

**Feature 4.8: Agent 5 - Compliance Auditor**
- Check format compliance (4 volumes, page limits, fonts)
- Check content compliance (all requirements addressed)
- Score against Section M evaluation criteria
- Generate compliance report
- Store in `proposal_jobs_summary.compliance_report` (jsonb)

**Feature 4.9: Agent 6 - Humanization**
- Scan for AI buzzwords ("proven track record" × 7)
- Fix metric inconsistencies (99.95% vs 99.97%)
- Vary sentence structure
- Generate QA report

**UI Component: Compliance Dashboard**
```
COMPLIANCE AUDIT COMPLETE

FORMAT: ✅ 9/10 checks passed
⚠️ Volume I is 52 pages (limit: 50) → Cut 2 pages

CONTENT: ✅ 98% requirements addressed
❌ Missing CMMC cert documentation → Add to Vol I

SCORE: 83% (Target: 85%+)
⚠️ Need to improve Factor 1 (Technical) by 2 points

[View Full Report] [Proceed to Revision]
```

---

### **PHASE 5: REVISION & FINALIZATION** (Agent 7 + 8)

**Feature 4.10: Agent 7 - Revision Handler**
- Implement fixes from Agent 5/6 automatically
- Present draft to human for review
- Handle client feedback via comment system
- Re-run compliance check after revisions

**UI Component: Review Interface**
```
VOLUME I - TECHNICAL (Page 12)

[PDF Preview - left side]

[Comments - right side]
👤 User: "We don't use Tool X, replace with Tool Y"
🤖 Agent 7: ✅ Updated (3 instances replaced)

👤 User: "We've done 12 migrations, not 15"
🤖 Agent 7: ✅ Corrected + updated related metrics

[Approve Volume] [Request More Changes]
```

**Feature 4.11: Agent 8 - Packaging**
- Apply professional formatting (headers, footers, TOC)
- Generate cover pages
- Convert to PDFs
- Package deliverables:
  - Submission package (4 PDFs + Excel)
  - Archive package (4 DOCX + compliance matrix)
- Store in `proposal_jobs_summary` URLs

**UI Component: Final Deliverables**
```
✅ PROPOSAL READY FOR SUBMISSION

SUBMISSION PACKAGE:
📄 Volume_I_Technical_FINAL.pdf (4.2 MB)
📄 Volume_II_Management_FINAL.pdf (2.1 MB)
📄 Volume_III_PastPerformance_FINAL.pdf (1.8 MB)
📄 Volume_IV_Price_FINAL.pdf (0.9 MB)
📊 Excel_Cost_Template.xlsx (0.3 MB)

[Download All as ZIP] [Download Individual Files]

ARCHIVE PACKAGE (Editable):
📝 Volume_I_Technical_FINAL.docx
[Continue...]

[Mark as Submitted] [Start New Proposal]
```

---

## 5. AGENT ORCHESTRATION WORKFLOW

### **High-Level Flow**
```
1. User uploads RFP
   ↓
2. Agent 0 creates 4 volume containers
   ↓
3. Agent 1 parses RFP → compliance matrix
   ↓
4. User completes intake form (or uses existing data)
   ↓
5. Agent 2 validates data → report
   ↓
   [If BLOCKERS] → User fixes data → retry Agent 2
   ↓
6. Agent 3 generates outlines
   ↓
7. Agent 4 (4A/4B/4C/4D) writes volumes in parallel
   ↓
8. Agent 5 audits compliance → report
   ↓
9. Agent 6 humanizes content → QA report
   ↓
10. Agent 7 implements fixes
   ↓
11. User reviews drafts → provides feedback
   ↓
12. Agent 7 implements user feedback
   ↓
13. Agent 8 packages final deliverables
   ↓
14. User downloads and submits
```

### **Technical Implementation**
- **Job Queue:** Use Supabase Edge Functions + pg_cron for agent orchestration
- **State Machine:** Store current agent + status in `proposal_jobs.current_agent`
- **Parallel Processing:** Agents 4A/4B/4C/4D run concurrently using Promise.all()
- **Error Handling:** If agent fails, store error in `proposal_jobs.error_log`, allow retry

**Database Updates:**
```sql
ALTER TABLE proposal_jobs ADD COLUMN current_agent text; -- 'agent_0', 'agent_1'...
ALTER TABLE proposal_jobs ADD COLUMN agent_progress jsonb; -- {agent_1: 'complete', agent_2: 'running'}
ALTER TABLE proposal_jobs ADD COLUMN error_log jsonb[]; -- [{agent: 'agent_5', error: '...', timestamp}]
```

---

## 6. USER INTERFACE MOCKUPS

### **6.1: Dashboard (Home)**
```
┌─────────────────────────────────────────────────────┐
│ 📊 PROPOSALS DASHBOARD                    [+ New]   │
├─────────────────────────────────────────────────────┤
│                                                      │
│ ┌─────────────────────────────────────────┐         │
│ │ 🚧 In Progress (3)                      │         │
│ │                                          │         │
│ │ RFP-2025-1117 - UVU LMS                │         │
│ │ Status: Agent 5 - Compliance Audit      │         │
│ │ Due: Feb 1, 2025 (52 days)              │         │
│ │ [Continue] [View Report]                │         │
│ │                                          │         │
│ │ RFP-2025-0089 - DoD Cloud Migration    │         │
│ │ Status: Agent 4 - Writing (65%)         │         │
│ │ Due: Mar 1, 2025 (81 days)              │         │
│ │ [Continue]                               │         │
│ └──────────────────────────────────────────┘         │
│                                                      │
│ ┌─────────────────────────────────────────┐         │
│ │ ✅ Complete (12)                        │         │
│ │ [View All]                               │         │
│ └──────────────────────────────────────────┘         │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### **6.2: Proposal Creation Wizard**
```
┌─────────────────────────────────────────────────────┐
│ CREATE NEW PROPOSAL                                  │
├─────────────────────────────────────────────────────┤
│                                                      │
│ Step 1 of 3: Upload RFP                             │
│                                                      │
│ ┌──────────────────────────────────────┐            │
│ │  📄 Drag & drop RFP PDF here         │            │
│ │     or click to browse                │            │
│ │                                       │            │
│ │     Max 50 MB, PDF only               │            │
│ └──────────────────────────────────────┘            │
│                                                      │
│ RFP Details:                                         │
│ RFP Number: [____________]                           │
│ RFP Title: [_________________________]               │
│ Due Date: [__/__/____]                               │
│                                                      │
│ [Back]                           [Next: Parse RFP] →│
└─────────────────────────────────────────────────────┘
```

### **6.3: Agent Progress View**
```
┌─────────────────────────────────────────────────────┐
│ RFP-2025-1117 - UVU LMS                              │
│ Status: ✅ Agent 3 Complete → 🔄 Agent 4 Running    │
├─────────────────────────────────────────────────────┤
│                                                      │
│ Agent Pipeline:                                      │
│ ✅ Agent 0 - Volume Creation                        │
│ ✅ Agent 1 - RFP Parsing                            │
│ ✅ Agent 2 - Data Validation (3 warnings)          │
│ ✅ Agent 3 - Content Mapping                        │
│ 🔄 Agent 4 - Content Generation (67%)               │
│    └─ 4A: Vol I Technical (40/50 pages)             │
│    └─ 4B: Vol II Management (22/30 pages)           │
│    └─ 4C: Vol III Past Perf (17/25 pages)           │
│    └─ 4D: Vol IV Price (10 pages)                   │
│ ⏸️  Agent 5 - Compliance Audit (pending)            │
│ ⏸️  Agent 6 - Humanization (pending)                │
│ ⏸️  Agent 7 - Revision (pending)                    │
│ ⏸️  Agent 8 - Packaging (pending)                   │
│                                                      │
│ Estimated time remaining: 4 minutes                  │
│                                                      │
│ [View Logs] [Pause] [Cancel]                        │
└─────────────────────────────────────────────────────┘
```

---

## 7. API ENDPOINTS (Server Actions / Edge Functions)
```typescript
// RFP Management
POST   /api/proposals/create           // Create new proposal job
POST   /api/proposals/:id/upload-rfp   // Upload RFP PDF
GET    /api/proposals/:id              // Get proposal details
PATCH  /api/proposals/:id/status       // Update status

// Agent Orchestration
POST   /api/agents/run                 // Trigger agent execution
GET    /api/agents/:proposal_id/status // Get agent progress
POST   /api/agents/:proposal_id/retry  // Retry failed agent

// Data Management
GET    /api/companies/:id/intake       // Get intake form data
POST   /api/companies/:id/intake       // Submit intake form
GET    /api/companies/:id/validate     // Run Agent 2 validation

// Review & Feedback
POST   /api/proposals/:id/feedback     // Submit user feedback
GET    /api/proposals/:id/drafts       // Get draft volumes
GET    /api/proposals/:id/reports      // Get compliance reports

// Deliverables
GET    /api/proposals/:id/deliverables // Get final files
POST   /api/proposals/:id/package      // Trigger Agent 8 packaging
```

---

## 8. SUCCESS CRITERIA & TESTING

### **Functional Requirements**
✅ System generates 4 separate volumes (not 1 combined)
✅ All volumes meet page limits
✅ All mandatory requirements addressed (100% coverage)
✅ No placeholder data in final output
✅ Compliance score ≥85% on expert review
✅ Human can provide feedback and see revisions implemented

### **Non-Functional Requirements**
✅ RFP parsing completes in < 2 minutes
✅ Full proposal generation in < 15 minutes
✅ System handles RFPs up to 50MB
✅ 99% uptime for production
✅ All sensitive data encrypted at rest

### **User Acceptance Testing**
1. Upload test RFP (DoD_Cloud_Migration_RFP.pdf)
2. Complete intake form with real company data
3. Run through full 8-agent pipeline
4. Verify 4 separate PDFs generated
5. Have expert score output (target: 85%+)
6. Compare time: manual (80 hrs) vs. automated (10 hrs)

---

## 9. ROLLOUT PLAN

**Week 1-2:** Phase 1 (Agents 0, 1)
- Build RFP upload + parsing
- Create volume containers
- Test with 3 sample RFPs

**Week 3-4:** Phase 2 (Agent 2)
- Build intake form (all 7 sections)
- Implement validation logic
- Test with real company data

**Week 5-6:** Phase 3 (Agents 3, 4)
- Build content mapping
- Implement parallel writing (4 sub-agents)
- Test with full proposal generation

**Week 7:** Phase 4 (Agents 5, 6)
- Build compliance auditor
- Build humanization pass
- Test scoring accuracy

**Week 8:** Phase 5 (Agents 7, 8)
- Build revision handler
- Build packaging & formatting
- Full end-to-end test

**Week 9:** UAT + Expert Review
**Week 10:** Launch 🚀